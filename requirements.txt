# ophyd with epicsrs backend support (feature/epicsrs-backend branch)
ophyd @ git+https://github.com/physwkim/ophyd.git@feature/epicsrs-backend

# Rust CA backend for ophyd (built from ophyd repo's epicsrs/ subdirectory)
ophyd-epicsrs @ git+https://github.com/physwkim/ophyd-epicsrs.git

# Async MongoDB writer with Rust backend (latest)
bluesky-dataforge @ git+https://github.com/physwkim/bluesky-dataforge.git

# Core dependencies
bluesky>=1.13
numpy>=2.2
pymongo>=4.10
